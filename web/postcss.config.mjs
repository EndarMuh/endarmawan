// Tailwind removed: this portfolio uses hand-written CSS (globals.css), and Tailwind v4's
// lightningcss native binary has no 32-bit Windows build. No PostCSS plugins are needed.
const config = {
  plugins: {},
};

export default config;
